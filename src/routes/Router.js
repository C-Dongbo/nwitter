import { HashRouter as Router, Route, Switch} from "react-router-dom"

const AppRouter = () => {
    return(
        <Router>
            <switch>
                <Route />
            </switch>
        </Router>
    );
};

export default AppRouter;